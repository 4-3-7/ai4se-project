/**
 * LLM Provider abstract interface.
 * Re-exports the LLMProvider interface from types.
 * Corresponds to SPEC §3.2.
 */

export type { LLMProvider, LLMResponse, LLMOptions, Message, ToolCall } from './types.js';